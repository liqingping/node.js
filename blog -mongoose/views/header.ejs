<!DOCTYPE html>
<html>
<head>
    <title>博客</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
<header>
    <script type="text/javascript">
        function loadXMLDoc()
        {
            var xmlhttp;
            if (window.XMLHttpRequest)
            {// code for IE7+, Firefox, Chrome, Opera, Safari
                xmlhttp=new XMLHttpRequest();
            }
            else
            {// code for IE6, IE5
                xmlhttp=new ActiveXObject("Microsoft.XMLHTTP");
            }
            xmlhttp.onreadystatechange=function()
            {
                if (xmlhttp.readyState==4 && xmlhttp.status==200)
                {
                    document.getElementById("canvas").setAttribute('src',xmlhttp.responseText);

                }
            }
            xmlhttp.open("GET","/cava",true);
            xmlhttp.send();
        }
    </script>
<h1><%= title%></h1>
</header>
<nav>
    <span><a title="主页" href="/">主页</a> </span>
    <%if(user){%>
    <span><a title="发表" href="/post">发表</a> </span>
    <span><a title="登出" href="/logout">登出</a> </span>
    <span><a title="账户设置" href="/user">账户设置</a> </span>
    <%}else{%>
    <span><a title="注册" href="/reg">注册</a> </span>
    <span><a title="登陆" href="/login">登陆</a> </span>
    <%}%>
</nav>
<article>
    <%if(success){%>
    <div><b><%= success%></b></div>
    <%}%>
    <%if(error){%>
    <div><a><%=error %></a></div>
    <%}%>